// On page load 
    // get route for all user workouts > render in workouts column

// Submit Workout Button
    // listner > post route to add workout and reload page

// Go Workout button
    // listner > route to grab workout information and load exercise page